# Spring Modulith

This document outlines how Spring Modulith principles are applied in this template project to create a well-structured, modular application architecture.

## What is Spring Modulith?

Spring Modulith is a framework that extends the Spring Boot ecosystem to support the development of modular monolithic applications. It enables creating well-structured, domain-aligned
applications with clear boundaries between modules while maintaining the simplicity of a monolithic deployment model.

Spring Modulith helps with:

- Defining logical application modules
- Enforcing module boundaries
- Testing modules in isolation
- Documenting module structures and dependencies
- Enabling event-driven communication between modules

## Core Principles

### Module Definition

Modules are defined through the package structure of the application:
